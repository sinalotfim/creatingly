import { ImageObserveDirective } from './image-observe.directive';

describe('ImageObserveDirective', () => {
	it('should create an instance', () => {
		const directive = new ImageObserveDirective();
		expect(directive).toBeTruthy();
	});
});
