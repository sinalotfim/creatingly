import { LoaderObserveDirective } from './loader-observe.directive';

describe('LoaderObserveDirective', () => {
	it('should create an instance', () => {
		const directive = new LoaderObserveDirective();
		expect(directive).toBeTruthy();
	});
});
